import {
  IonBackButton,
  IonButtons,
  IonContent,
  IonHeader,
  IonPage,
  IonTitle,
  IonToolbar,
} from '@ionic/react';
import { Suspense } from 'react';
import { IonLoadingSpinner } from "@repo/ionic/IonLoadingSpinner";

export default function {{ pascalCase name }}Page() {
  return (
       <IonPage>
      <IonHeader>
        <IonToolbar>
          <IonButtons slot='start'>
            <IonBackButton></IonBackButton>
          </IonButtons>
          <IonTitle>{{ titleCase name }}</IonTitle>
        </IonToolbar>
      </IonHeader>
      <IonContent>
        <Suspense fallback={<IonLoadingSpinner />}>
         <div className='full centered'>{{ titleCase name }}</div>
        </Suspense>
      </IonContent>
    </IonPage>
  );
};

// generated