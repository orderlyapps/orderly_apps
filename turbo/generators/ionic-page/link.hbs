import { IonItem } from "@ionic/react";
import { ComponentProps } from "react";
import {
  path,
  {{ pascalCase name }}PageParams,
} from "./use{{ pascalCase name }}PageParams";

export function {{ pascalCase name }}PageLinkItem({
  param,
  ...props
}: ComponentProps<typeof IonItem> & { param?: {{ pascalCase name }}PageParams }) {
  return (
    <IonItem routerLink={path(param)} {...props}>
      {props.children || "{{ titleCase name }}"}
    </IonItem>
  );
}

//generated using ionic-page/link.hbs
