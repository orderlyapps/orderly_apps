import { {{ constantCase app }}_PATHS } from "../{{ constantCase app }}_PATHS";

export type Param = string | undefined | null;

export const path = (param?: Param) => {
  if (param) return `${ {{ constantCase app }}_PATHS.{{snakeCase name}} }/${param}`;
  return {{ constantCase app }}_PATHS.{{snakeCase name}};
};

// generated