import { Ion{{ pascalCase type }} } from "@ionic/react";
import { path, Param } from "../../helper/path";
import { ComponentProps } from "react"
import { {{ constantCase app }}_PATHS } from "../{{ constantCase app }}_PATHS";

export function {{pascalCase name}}PageLink{{ pascalCase type }}({
  param, 
  ...props
}: ComponentProps<typeof Ion{{ pascalCase type }}> & { param?: Param }) {
  return (
    <Ion{{ pascalCase type }} routerLink={path( {{ constantCase app }}_PATHS, "{{snakeCase name}}", param)} {...props}>
      {props.children || "{{titleCase name}}"}
    </Ion{{ pascalCase type }}>
  );
}

// generated
